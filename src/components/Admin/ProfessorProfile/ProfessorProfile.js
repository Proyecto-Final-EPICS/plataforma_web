import {useState} from 'react';

export default function ProfessorProfile (props) {
    const {setIsVisibleModal, info} = props;
    const {name, srcPhoto, degree, phone, email, description} = info;

    return (
        <>

        </>
    )
}